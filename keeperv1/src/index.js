import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

// add notes with a title and push it to a new array. 
// * create a constant that keeps track of the title and content
// * pass the new note back to the App.jsx
// * Add new note to an array. 
//* Take array and render seperate Note Component for each item in array


// be able to click on delete button
//* Callback from the Note component to trigger a delete function 
//* Use the filter function to filer out the item that needs deletion
// * Pass a id over to the Note component, pass it back to the App when clicked